# Network interface to capture traffic from
interface="wlp2s0"

# Duration of the capture in seconds
duration=20

# Optional filter to apply during capture (e.g., "tcp port 80 or port 443")
filter="tcp port 80 or port 443"
